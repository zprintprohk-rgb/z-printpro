# Netlify 備用重定向規則（與 netlify.toml 雙重保險）

# 國際化路由支持
/zh-hk/*  /zh-hk/index.html  200
/en/*     /en/index.html     200

# API 路由（不干擾 Functions）
/api/*    /.netlify/functions/:splat  200

# SPA 回退（所有路由到 index.html）
/*        /index.html        200